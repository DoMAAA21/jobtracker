# Development image — bind-mount source; see root docker-compose.yml
FROM node:22-alpine

WORKDIR /app

RUN apk add --no-cache libc6-compat

COPY package.json package-lock.json ./
COPY prisma ./prisma
COPY prisma.config.ts ./
RUN npm ci

EXPOSE 3000

CMD ["npm", "run", "start:dev"]